# RabbitMQ configuration for testing
# Suppress most logs to keep test output clean

# Log levels
log.console.level = error
log.file.level = error

# Basic settings
default_user = admin
default_pass = password
default_vhost = /

# Disable some features that generate logs
collect_statistics_interval = 30000
